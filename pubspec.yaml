name: news_app
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  dio: ^5.7.0
  shared_preferences: ^2.3.0
  cached_network_image: ^3.4.1
  share_plus: ^11.0.0  # ترقية إلى إصدار أحدث
  url_launcher: ^6.1.0
  http: ^1.2.0
  email_validator: ^3.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # assets:
  # - assets/default_avatar.jpg